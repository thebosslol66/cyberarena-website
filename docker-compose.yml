services:
  website:
    build:
      context: .
      dockerfile: Dockerfile
    image: cyberarena-website:latest
    restart: always
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:8000

